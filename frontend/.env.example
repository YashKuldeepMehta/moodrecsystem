# Backend API (when running with Docker)
VITE_API_URL=/api/v1

# TMDB API key (free at https://www.themoviedb.org/settings/api)
# If left empty, fallback movie list is used — app still fully works without it
VITE_TMDB_API_KEY=0fd4eb9ea6f5be9e82ab885e20de0e5b
